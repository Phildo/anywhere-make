#!/bin/bash

while [ "$PWD" != "/" ];  do
  for i in *; do
    if [[ $i =~ [Mm]akefile|MAKEFILE ]] && [ -f $i ]; then
      echo "`pwd`/$i"
      exit 0
    fi
  done
  cd ..
done

echo "No make file was found in this or any parent directories" 1>&2
exit 1
